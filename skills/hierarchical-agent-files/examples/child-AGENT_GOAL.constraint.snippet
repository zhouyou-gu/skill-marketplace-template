- The parent `{{ parent_name }}/` mission remains the outer boundary for this workspace.
- This child mission may narrow or partition the parent mission, but it must not expand, reinterpret, or override the parent mission.
- Current-turn chat or user instructions are supporting context only; they do not persistently amend parent or child scope unless the user explicitly approves goal-file content.
